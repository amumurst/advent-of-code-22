Requires scala-cli to be set up https://scala-cli.virtuslab.org/install

Run with `scala-cli . --main-class NAME_OF_MAIN` or `scala-cli . --interactive`

Optionally run with `--native` to compile to native code
